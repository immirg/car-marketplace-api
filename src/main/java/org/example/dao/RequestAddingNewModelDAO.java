package org.example.dao;

import org.example.entity.RequestAddingNewModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestAddingNewModelDAO extends JpaRepository<RequestAddingNewModel, Integer> {
}
